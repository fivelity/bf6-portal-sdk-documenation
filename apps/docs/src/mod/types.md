---
sidebar: auto
---

# Mod Types

All **40 structured and interface definitions** in the `mod` namespace. Types are opaque by default.

## Overview

Types are declared in `types.d.ts` and globally accessible:

```typescript
const player: mod.Player;
const weapon: mod.WeaponPackage;
```

---

::: info

Type documentation is auto-generated from upstream `.d.ts` files.

:::
