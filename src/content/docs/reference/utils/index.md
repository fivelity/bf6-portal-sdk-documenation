---
title: utils API Reference
description: Auto-generated TypeDoc reference for bf6-portal-utils.
editUrl: false
---

:::note
This page and everything below it in the sidebar is generated by
`pnpm run docs:api:utils` (TypeDoc + `typedoc-plugin-markdown`) reading
directly from the installed `bf6-portal-utils` package. It regenerates on
every CI build — do not hand-edit files under this directory, they will be
overwritten.
:::

Run the generator locally before `astro dev` to populate this section:

```bash
pnpm run docs:api:utils
```

If you're viewing this placeholder instead of generated content, the
generator either hasn't been run yet or found nothing to document — check
that `bf6-portal-utils` is installed and that
`node_modules/bf6-portal-utils/index.d.ts` (or the entry point configured in
`typedoc/utils.json`) resolves correctly.
